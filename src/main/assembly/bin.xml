<?xml version="1.0" encoding="UTF-8"?>
<!-- Assembly descriptor for h2o.jar -->
<assembly>
	<id>bin</id>
	<formats>
		<format>jar</format>
	</formats>
	<!-- Do not make top-level directory in jar. -->
	<includeBaseDirectory>false</includeBaseDirectory>
	<!-- Put all runtime dependencies into lib directory divided per artifact's 
		groupId -->
	<dependencySets>
		<dependencySet>
			<!-- Do not copy whole project tree into resulting jar -->
			<useProjectArtifact>false</useProjectArtifact>
			<useTransitiveDependencies>true</useTransitiveDependencies>
			<outputDirectory>/lib</outputDirectory>
			<directoryMode>0755</directoryMode>
			<outputFileNameMapping>${artifact.groupId}/${artifact.artifactId}-${artifact.version}${dashClassifier?}.${artifact.extension}</outputFileNameMapping>
			<scope>runtime</scope>
			<excludes>
				<exclude>org.javassist:javassist</exclude>
				<!-- <exclude>org.apache.hadoop:hadoop-client</exclude>-->
			</excludes>
			<!-- Transitively filter hadoop and javassis dependencies. -->
			<useTransitiveFiltering>true</useTransitiveFiltering>
		</dependencySet>
		<dependencySet>
			<outputDirectory>/</outputDirectory>
			<useProjectArtifact>false</useProjectArtifact>
			<includes>
				<include>org.javassist:javassist</include>
			</includes>
			<unpack>true</unpack>
		</dependencySet>
	</dependencySets>
	<fileSets>
		<fileSet>
			<directory>target/classes</directory>
			<outputDirectory></outputDirectory>
		</fileSet>
	</fileSets>
</assembly>
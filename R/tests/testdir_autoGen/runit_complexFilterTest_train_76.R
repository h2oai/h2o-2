            ##
            # Author: Autogenerated on 2013-11-29 01:20:08
            # gitHash: 10810480518d43dd720690e729d2f3b9a0f8eba7
            # SEED: 7977475956795433203
            ##
            source('./findNSourceUtils.R')
            Log.info("======================== Begin Test ===========================")
            complexFilterTest_train_76 <- function(conn) {
                Log.info("A munge-task R unit test on data <train> testing the functional unit <['', '==']> ")
                Log.info("Uploading train")
                hex <- h2o.uploadFile(conn, locate("../../smalldata/chess/chess_2x2x1000/weka/train.csv.arff"), "rtrain.hex")
            Log.info("Performing compound task ( ( hex[,c(1)] == 0.70394668154 ))  on dataset <train>")
                     filterHex <- hex[( ( hex[,c(1)] == 0.70394668154 )) ,]
            Log.info("Performing compound task ( ( hex[,c(1)] == 1.49505953641 ))  on dataset train, and also subsetting columns.")
                     filterHex <- hex[( ( hex[,c(1)] == 1.49505953641 )) , c(1)]
                Log.info("Now do the same filter & subset, but select complement of columns.")
                     filterHex <- hex[( ( hex[,c(1)] == 1.49505953641 )) , c(2)]
            Log.info("End of test")
            PASSS <<- TRUE
            }
            PASSS <- FALSE
            conn = new("H2OClient", ip=myIP, port=myPort)
            tryCatch(test_that("compoundFilterTest_ on data train", complexFilterTest_train_76(conn)), warning = function(w) WARN(w), error = function(e) FAIL(e))
            if (!PASSS) FAIL("Did not reach the end of test. Check Rsandbox/errors.log for warnings and errors.")
            PASS()

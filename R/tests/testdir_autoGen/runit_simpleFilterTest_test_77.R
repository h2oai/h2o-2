            ##
            # Author: Autogenerated on 2013-11-27 17:26:17
            # gitHash: a4fe16d99eb5f003dd7c7d0da17f29e1712eda0f
            # SEED: 8586424275721420395
            ##
            source('./findNSourceUtils.R')
            Log.info("======================== Begin Test ===========================")
            simpleFilterTest_test_77 <- function(conn) {
                Log.info("A munge-task R unit test on data <test> testing the functional unit <>=> ")
                Log.info("Uploading test")
                hex <- h2o.uploadFile(conn, locate("../../smalldata/chess/chess_2x2x200/R/test.csv"), "rtest.hex")
                Log.info("Filtering out rows by >= from dataset test and column \"y\" using value 0.537114334562")
                     filterHex <- hex[hex[,c("y")] >= 0.537114334562,]
                    Log.info("Perform filtering with the '$' sign also")
                    filterHex <- hex[hex$"y" >= 0.537114334562,]
                Log.info("Filtering out rows by >= from dataset test and column \"y\" using value 1.55891980422")
                     filterHex <- hex[hex[,c("y")] >= 1.55891980422,]
                    Log.info("Perform filtering with the '$' sign also")
                    filterHex <- hex[hex$"y" >= 1.55891980422,]
                    Log.info("Filtering out rows by >= from dataset test and column \"x\" using value 0.839419497581, and also subsetting columns.")
                     filterHex <- hex[hex[,c("x")] >= 0.839419497581, c("x")]
                    Log.info("Now do the same filter & subset, but select complement of columns.")
                     filterHex <- hex[hex[,c("x")] >= 0.839419497581, c("color","x","y")]
                    Log.info("Filtering out rows by >= from dataset test and column \"x\" using value 0.508615433845, and also subsetting columns.")
                     filterHex <- hex[hex[,c("x")] >= 0.508615433845, c("x")]
                    Log.info("Now do the same filter & subset, but select complement of columns.")
                     filterHex <- hex[hex[,c("x")] >= 0.508615433845, c("color","x","y")]
            }
            conn = new("H2OClient", ip=myIP, port=myPort)
            tryCatch(test_that("simpleFilterTest_ on data test", simpleFilterTest_test_77(conn)), warning = function(w) WARN(w), error = function(e) FAIL(e))
            PASS()

            ##
            # Author: Autogenerated on 2013-11-29 01:20:08
            # gitHash: 10810480518d43dd720690e729d2f3b9a0f8eba7
            # SEED: 7977475956795433203
            ##
            source('./findNSourceUtils.R')
            Log.info("======================== Begin Test ===========================")
            complexFilterTest_test_69 <- function(conn) {
                Log.info("A munge-task R unit test on data <test> testing the functional unit <['', '==']> ")
                Log.info("Uploading test")
                hex <- h2o.uploadFile(conn, locate("../../smalldata/chess/chess_2x2x1000/h2o/test.csv"), "rtest.hex")
            Log.info("Performing compound task ( ( hex[,c(\"x\")] == 1.5469347786 ))  on dataset <test>")
                     filterHex <- hex[( ( hex[,c("x")] == 1.5469347786 )) ,]
            Log.info("Performing compound task ( ( hex[,c(\"x\")] == 1.33399535336 ))  on dataset test, and also subsetting columns.")
                     filterHex <- hex[( ( hex[,c("x")] == 1.33399535336 )) , c("x","y")]
                Log.info("Now do the same filter & subset, but select complement of columns.")
                     filterHex <- hex[( ( hex[,c("x")] == 1.33399535336 )) , c("color")]
            Log.info("End of test")
            PASSS <<- TRUE
            }
            PASSS <- FALSE
            conn = new("H2OClient", ip=myIP, port=myPort)
            tryCatch(test_that("compoundFilterTest_ on data test", complexFilterTest_test_69(conn)), warning = function(w) WARN(w), error = function(e) FAIL(e))
            if (!PASSS) FAIL("Did not reach the end of test. Check Rsandbox/errors.log for warnings and errors.")
            PASS()

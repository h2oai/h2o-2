            ##
            # Author: Autogenerated on 2013-12-05 22:49:50
            # gitHash: dbc23230f6666890a6cb7c4234e68e540e096e0c
            # SEED: 779905237530228736
            ##
            source('./findNSourceUtils.R')
            simpleFilterTest_poker10_8 <- function(conn) {
                Log.info("A munge-task R unit test on data <poker10> testing the functional unit <<> ")
                Log.info("Uploading poker10")
                hex <- h2o.uploadFile(conn, locate("../../smalldata/poker/poker10"), "rpoker10.hex")
                Log.info("Filtering out rows by < from dataset poker10 and column \"1\" using value 1.0")
                     filterHex <- hex[hex[,c(1)] < 1.0,]
                    Log.info("Perform filtering with the '$' sign also")
                    filterHex <- hex[hex$"1" < 1.0,]
                Log.info("Filtering out rows by < from dataset poker10 and column \"8\" using value 1.0")
                     filterHex <- hex[hex[,c(8)] < 1.0,]
                    Log.info("Perform filtering with the '$' sign also")
                    filterHex <- hex[hex$"8" < 1.0,]
                Log.info("Filtering out rows by < from dataset poker10 and column \"7\" using value 4.0")
                     filterHex <- hex[hex[,c(7)] < 4.0,]
                    Log.info("Perform filtering with the '$' sign also")
                    filterHex <- hex[hex$"7" < 4.0,]
                    Log.info("Filtering out rows by < from dataset poker10 and column \"6\" using value 1.0, and also subsetting columns.")
                     filterHex <- hex[hex[,c(6)] < 1.0, c(6)]
                    Log.info("Now do the same filter & subset, but select complement of columns.")
                     filterHex <- hex[hex[,c(6)] < 1.0, c(10,1,3,2,5,4,7,6,9,8)]
                    Log.info("Filtering out rows by < from dataset poker10 and column \"1\" using value 1.0, and also subsetting columns.")
                     filterHex <- hex[hex[,c(1)] < 1.0, c(1)]
                    Log.info("Now do the same filter & subset, but select complement of columns.")
                     filterHex <- hex[hex[,c(1)] < 1.0, c(10,1,3,2,5,4,7,6,9,8)]
                    Log.info("Filtering out rows by < from dataset poker10 and column \"5\" using value 3.0, and also subsetting columns.")
                     filterHex <- hex[hex[,c(5)] < 3.0, c(5)]
                    Log.info("Now do the same filter & subset, but select complement of columns.")
                     filterHex <- hex[hex[,c(5)] < 3.0, c(10,1,3,2,5,4,7,6,9,8)]
            testEnd()
            }
            doTest("simpleFilterTest_ on data poker10 unit= <", simpleFilterTest_poker10_8)

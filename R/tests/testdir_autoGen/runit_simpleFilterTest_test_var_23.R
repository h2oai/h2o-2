            ##
            # Author: Autogenerated on 2013-11-27 18:13:58
            # gitHash: c4ad841105ba82f4a3979e4cf1ae7e20a5905e59
            # SEED: 4663640625336856642
            ##
            source('./findNSourceUtils.R')
            Log.info("======================== Begin Test ===========================")
            simpleFilterTest_test_var_23 <- function(conn) {
                Log.info("A munge-task R unit test on data <test_var> testing the functional unit <<> ")
                Log.info("Uploading test_var")
                hex <- h2o.uploadFile(conn, locate("../../smalldata/test/test_var.csv"), "rtest_var.hex")
                Log.info("Filtering out rows by < from dataset test_var and column \"1\" using value 1.47228693432")
                     filterHex <- hex[hex[,c(1)] < 1.47228693432,]
                    Log.info("Perform filtering with the '$' sign also")
                    filterHex <- hex[hex$"1" < 1.47228693432,]
                Log.info("Filtering out rows by < from dataset test_var and column \"1\" using value 2.83284039267")
                     filterHex <- hex[hex[,c(1)] < 2.83284039267,]
                    Log.info("Perform filtering with the '$' sign also")
                    filterHex <- hex[hex$"1" < 2.83284039267,]
                    Log.info("Filtering out rows by < from dataset test_var and column \"0\" using value 1.0, and also subsetting columns.")
                     filterHex <- hex[hex[,c(1)] < 1.0, c(1)]
                    Log.info("Now do the same filter & subset, but select complement of columns.")
                     filterHex <- hex[hex[,c(1)] < 1.0, c(1)]
                    Log.info("Filtering out rows by < from dataset test_var and column \"0\" using value 1.0, and also subsetting columns.")
                     filterHex <- hex[hex[,c(1)] < 1.0, c(1)]
                    Log.info("Now do the same filter & subset, but select complement of columns.")
                     filterHex <- hex[hex[,c(1)] < 1.0, c(1)]
            }
            conn = new("H2OClient", ip=myIP, port=myPort)
            tryCatch(test_that("simpleFilterTest_ on data test_var", simpleFilterTest_test_var_23(conn)), warning = function(w) WARN(w), error = function(e) FAIL(e))
            PASS()

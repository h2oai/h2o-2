            ##
            # Author: Autogenerated on 2013-11-27 17:26:17
            # gitHash: a4fe16d99eb5f003dd7c7d0da17f29e1712eda0f
            # SEED: 8586424275721420395
            ##
            source('./findNSourceUtils.R')
            Log.info("======================== Begin Test ===========================")
            complexFilterTest_iris_wheader_154 <- function(conn) {
                Log.info("A munge-task R unit test on data <iris_wheader> testing the functional unit <['!', '==', '&', '>', '|', '>=']> ")
                Log.info("Uploading iris_wheader")
                hex <- h2o.uploadFile(conn, locate("../../smalldata/iris/iris_wheader.csv.gz"), "riris_wheader.hex")
            Log.info("Performing compound task !( ( hex[,c(\"sepal_wid\")] == 3.64222873072 ) & ( hex[,c(\"petal_len\")] > 4.67514574916 )) on dataset <iris_wheader>")
                     filterHex <- hex[!( ( hex[,c("sepal_wid")] == 3.64222873072 ) & ( hex[,c("petal_len")] > 4.67514574916 )),]
            Log.info("Performing compound task !( ( hex[,c(\"petal_len\")] == 6.07006248466 ) & ( hex[,c(\"petal_len\")] > 3.31643930222 )) on dataset iris_wheader, and also subsetting columns.")
                     filterHex <- hex[!( ( hex[,c("petal_len")] == 6.07006248466 ) & ( hex[,c("petal_len")] > 3.31643930222 )), c("petal_len")]
                Log.info("Now do the same filter & subset, but select complement of columns.")
                     filterHex <- hex[!( ( hex[,c("petal_len")] == 6.07006248466 ) & ( hex[,c("petal_len")] > 3.31643930222 )), c("petal_wid","sepal_wid","class","sepal_len")]
            }
            conn = new("H2OClient", ip=myIP, port=myPort)
            tryCatch(test_that("compoundFilterTest_ on data iris_wheader", complexFilterTest_iris_wheader_154(conn)), warning = function(w) WARN(w), error = function(e) FAIL(e))
            PASS()

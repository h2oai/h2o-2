            ##
            # Author: Autogenerated on 2013-11-29 01:20:08
            # gitHash: 10810480518d43dd720690e729d2f3b9a0f8eba7
            # SEED: 7977475956795433203
            ##
            source('./findNSourceUtils.R')
            Log.info("======================== Begin Test ===========================")
            complexFilterTest_test_77 <- function(conn) {
                Log.info("A munge-task R unit test on data <test> testing the functional unit <['', '==']> ")
                Log.info("Uploading test")
                hex <- h2o.uploadFile(conn, locate("../../smalldata/chess/chess_2x2x100/R/test.csv"), "rtest.hex")
            Log.info("Performing compound task ( ( hex[,c(\"x\")] == 0.441005034784 ))  on dataset <test>")
                     filterHex <- hex[( ( hex[,c("x")] == 0.441005034784 )) ,]
            Log.info("Performing compound task ( ( hex[,c(\"x\")] == 0.407354441757 ))  on dataset test, and also subsetting columns.")
                     filterHex <- hex[( ( hex[,c("x")] == 0.407354441757 )) , c("x","y")]
                Log.info("Now do the same filter & subset, but select complement of columns.")
                     filterHex <- hex[( ( hex[,c("x")] == 0.407354441757 )) , c("color")]
            Log.info("End of test")
            PASSS <<- TRUE
            }
            PASSS <- FALSE
            conn = new("H2OClient", ip=myIP, port=myPort)
            tryCatch(test_that("compoundFilterTest_ on data test", complexFilterTest_test_77(conn)), warning = function(w) WARN(w), error = function(e) FAIL(e))
            if (!PASSS) FAIL("Did not reach the end of test. Check Rsandbox/errors.log for warnings and errors.")
            PASS()

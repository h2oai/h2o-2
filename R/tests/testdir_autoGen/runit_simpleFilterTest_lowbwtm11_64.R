            ##
            # Author: Autogenerated on 2013-11-29 01:20:08
            # gitHash: 10810480518d43dd720690e729d2f3b9a0f8eba7
            # SEED: 7977475956795433203
            ##
            source('./findNSourceUtils.R')
            Log.info("======================== Begin Test ===========================")
            simpleFilterTest_lowbwtm11_64 <- function(conn) {
                Log.info("A munge-task R unit test on data <lowbwtm11> testing the functional unit <!=> ")
                Log.info("Uploading lowbwtm11")
                hex <- h2o.uploadFile(conn, locate("../../smalldata/logreg/umass_statdata/lowbwtm11.dat"), "rlowbwtm11.hex")
                Log.info("Filtering out rows by != from dataset lowbwtm11 and column \"UI\" using value 0.72625141642")
                     filterHex <- hex[hex[,c("UI")] != 0.72625141642,]
                    Log.info("Perform filtering with the '$' sign also")
                    filterHex <- hex[hex$"UI" != 0.72625141642,]
                    Log.info("Filtering out rows by != from dataset lowbwtm11 and column \"SMOKE\" using value 0.485892962834, and also subsetting columns.")
                     filterHex <- hex[hex[,c("SMOKE")] != 0.485892962834, c("SMOKE")]
                    Log.info("Now do the same filter & subset, but select complement of columns.")
                     filterHex <- hex[hex[,c("SMOKE")] != 0.485892962834, c("SMOKE","LOW","RACE","PTD","UI","Pair#","HT","LWT","AGE")]
            Log.info("End of test")
            PASSS <<- TRUE
            }
            PASSS <- FALSE
            conn = new("H2OClient", ip=myIP, port=myPort)
            tryCatch(test_that("simpleFilterTest_ on data lowbwtm11", simpleFilterTest_lowbwtm11_64(conn)), warning = function(w) WARN(w), error = function(e) FAIL(e))
            if (!PASSS) FAIL("Did not reach the end of test. Check Rsandbox/errors.log for warnings and errors.")
            PASS()

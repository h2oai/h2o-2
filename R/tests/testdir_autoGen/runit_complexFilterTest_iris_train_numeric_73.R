            ##
            # Author: Autogenerated on 2013-11-29 01:20:08
            # gitHash: 10810480518d43dd720690e729d2f3b9a0f8eba7
            # SEED: 7977475956795433203
            ##
            source('./findNSourceUtils.R')
            Log.info("======================== Begin Test ===========================")
            complexFilterTest_iris_train_numeric_73 <- function(conn) {
                Log.info("A munge-task R unit test on data <iris_train_numeric> testing the functional unit <['', '==']> ")
                Log.info("Uploading iris_train_numeric")
                hex <- h2o.uploadFile(conn, locate("../../smalldata/test/classifier/iris_train_numeric.csv"), "riris_train_numeric.hex")
            Log.info("Performing compound task ( ( hex[,c(\"petal_wid\")] == 1.78701979572 ))  on dataset <iris_train_numeric>")
                     filterHex <- hex[( ( hex[,c("petal_wid")] == 1.78701979572 )) ,]
            Log.info("Performing compound task ( ( hex[,c(\"sepal_wid\")] == 3.45546183751 ))  on dataset iris_train_numeric, and also subsetting columns.")
                     filterHex <- hex[( ( hex[,c("sepal_wid")] == 3.45546183751 )) , c("petal_wid","sepal_wid","petal_len","sepal_len","species")]
                Log.info("Now do the same filter & subset, but select complement of columns.")
                     filterHex <- hex[( ( hex[,c("sepal_wid")] == 3.45546183751 )) , c(1)]
            Log.info("End of test")
            PASSS <<- TRUE
            }
            PASSS <- FALSE
            conn = new("H2OClient", ip=myIP, port=myPort)
            tryCatch(test_that("compoundFilterTest_ on data iris_train_numeric", complexFilterTest_iris_train_numeric_73(conn)), warning = function(w) WARN(w), error = function(e) FAIL(e))
            if (!PASSS) FAIL("Did not reach the end of test. Check Rsandbox/errors.log for warnings and errors.")
            PASS()

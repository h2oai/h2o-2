            ##
            # Author: Autogenerated on 2013-11-27 18:13:59
            # gitHash: c4ad841105ba82f4a3979e4cf1ae7e20a5905e59
            # SEED: 4663640625336856642
            ##
            source('./findNSourceUtils.R')
            Log.info("======================== Begin Test ===========================")
            simpleFilterTest_train_128 <- function(conn) {
                Log.info("A munge-task R unit test on data <train> testing the functional unit <!=> ")
                Log.info("Uploading train")
                hex <- h2o.uploadFile(conn, locate("../../smalldata/chess/chess_2x2x10/weka/train.csv.arff"), "rtrain.hex")
                Log.info("Filtering out rows by != from dataset train and column \"0\" using value 0.286801784197")
                     filterHex <- hex[hex[,c(1)] != 0.286801784197,]
                    Log.info("Perform filtering with the '$' sign also")
                    filterHex <- hex[hex$"0" != 0.286801784197,]
                Log.info("Filtering out rows by != from dataset train and column \"0\" using value 1.46538385785")
                     filterHex <- hex[hex[,c(1)] != 1.46538385785,]
                    Log.info("Perform filtering with the '$' sign also")
                    filterHex <- hex[hex$"0" != 1.46538385785,]
                    Log.info("Filtering out rows by != from dataset train and column \"0\" using value 0.364835335068, and also subsetting columns.")
                     filterHex <- hex[hex[,c(1)] != 0.364835335068, c(1)]
                    Log.info("Now do the same filter & subset, but select complement of columns.")
                     filterHex <- hex[hex[,c(1)] != 0.364835335068, c(1,2)]
                    Log.info("Filtering out rows by != from dataset train and column \"1\" using value 0.67483662904, and also subsetting columns.")
                     filterHex <- hex[hex[,c(1)] != 0.67483662904, c(1)]
                    Log.info("Now do the same filter & subset, but select complement of columns.")
                     filterHex <- hex[hex[,c(1)] != 0.67483662904, c(1,2)]
            }
            conn = new("H2OClient", ip=myIP, port=myPort)
            tryCatch(test_that("simpleFilterTest_ on data train", simpleFilterTest_train_128(conn)), warning = function(w) WARN(w), error = function(e) FAIL(e))
            PASS()

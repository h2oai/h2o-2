            ##
            # Author: Autogenerated on 2013-11-27 17:26:17
            # gitHash: a4fe16d99eb5f003dd7c7d0da17f29e1712eda0f
            # SEED: 8586424275721420395
            ##
            source('./findNSourceUtils.R')
            Log.info("======================== Begin Test ===========================")
            complexFilterTest_iris_143 <- function(conn) {
                Log.info("A munge-task R unit test on data <iris> testing the functional unit <['!', '!=']> ")
                Log.info("Uploading iris")
                hex <- h2o.uploadFile(conn, locate("../../smalldata/iris/iris.csv.gz"), "riris.hex")
            Log.info("Performing compound task !( ( ( hex[,c(3)] != 0.62292238455 )) ) on dataset <iris>")
                     filterHex <- hex[!( ( ( hex[,c(3)] != 0.62292238455 )) ),]
            Log.info("Performing compound task !( ( ( hex[,c(3)] != 2.28392848233 )) ) on dataset iris, and also subsetting columns.")
                     filterHex <- hex[!( ( ( hex[,c(3)] != 2.28392848233 )) ), c(1,3,2)]
                Log.info("Now do the same filter & subset, but select complement of columns.")
                     filterHex <- hex[!( ( ( hex[,c(3)] != 2.28392848233 )) ), c(4)]
            }
            conn = new("H2OClient", ip=myIP, port=myPort)
            tryCatch(test_that("compoundFilterTest_ on data iris", complexFilterTest_iris_143(conn)), warning = function(w) WARN(w), error = function(e) FAIL(e))
            PASS()

            ##
            # Author: Autogenerated on 2013-11-27 03:20:00
            # gitHash: 25eb0cfd1269362ae85d0f073d9abde2a68ac0e4
            # SEED: 4617575514352894151
            ##
            source('./findNSourceUtils.R')
            Log.info("======================== Begin Test ===========================")
            complexFilterTest_constantColumn_161 <- function(conn) {
                Log.info("A munge-task R unit test on data <constantColumn> testing the functional unit <['!', '>', '&', '<=', '&', '!|', '!', '==']> ")
                Log.info("Uploading constantColumn")
                hex <- h2o.uploadFile(conn, "../../smalldata/constantColumn.csv", "rconstantColumn.hex")
            Log.info("Performing compound task !( ( hex[,c(\"konstant\")] > 0.1 ) & ( hex[,c(\"konstant\")] <= 0.1 ) & ( hex[,c(\"konstant\")] !| 0.1 ) ! ( hex[,c(\"konstant\")] == 0.1 )) on dataset <constantColumn>")
                     filterHex <- hex[!( ( hex[,c("konstant")] > 0.1 ) & ( hex[,c("konstant")] <= 0.1 ) & ( hex[,c("konstant")] !| 0.1 ) ! ( hex[,c("konstant")] == 0.1 )),]
            Log.info("Performing compound task !( ( hex[,c(\"konstant\")] > 0.1 ) & ( hex[,c(\"konstant\")] <= 0.1 ) & ( hex[,c(\"konstant\")] !| 0.1 ) ! ( hex[,c(\"konstant\")] == 0.1 )) on dataset constantColumn, and also subsetting columns.")
                     filterHex <- hex[!( ( hex[,c("konstant")] > 0.1 ) & ( hex[,c("konstant")] <= 0.1 ) & ( hex[,c("konstant")] !| 0.1 ) ! ( hex[,c("konstant")] == 0.1 )), c("konstant")]
                Log.info("Now do the same filter & subset, but select complement of columns.")
                     filterHex <- hex[!( ( hex[,c("konstant")] > 0.1 ) & ( hex[,c("konstant")] <= 0.1 ) & ( hex[,c("konstant")] !| 0.1 ) ! ( hex[,c("konstant")] == 0.1 )), c(1)]
            }
            conn = new("H2OClient", ip=myIP, port=myPort)
            tryCatch(test_that("compoundFilterTest_ on data constantColumn", complexFilterTest_constantColumn_161(conn)), warning = function(w) WARN(w), error = function(e) FAIL(e))
            PASS()

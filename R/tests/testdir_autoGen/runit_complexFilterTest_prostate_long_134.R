            ##
            # Author: Autogenerated on 2013-11-27 17:26:17
            # gitHash: a4fe16d99eb5f003dd7c7d0da17f29e1712eda0f
            # SEED: 8586424275721420395
            ##
            source('./findNSourceUtils.R')
            Log.info("======================== Begin Test ===========================")
            complexFilterTest_prostate_long_134 <- function(conn) {
                Log.info("A munge-task R unit test on data <prostate_long> testing the functional unit <['!', '!=']> ")
                Log.info("Uploading prostate_long")
                hex <- h2o.uploadFile(conn, locate("../../smalldata/logreg/prostate_long.csv.gz"), "rprostate_long.hex")
            Log.info("Performing compound task !( ( ( hex[,c(\"CAPSULE\")] != 0.0494749764467 )) ) on dataset <prostate_long>")
                     filterHex <- hex[!( ( ( hex[,c("CAPSULE")] != 0.0494749764467 )) ),]
            Log.info("Performing compound task !( ( ( hex[,c(\"PSA\")] != 51.2906042845 )) ) on dataset prostate_long, and also subsetting columns.")
                     filterHex <- hex[!( ( ( hex[,c("PSA")] != 51.2906042845 )) ), c("PSA","CAPSULE")]
                Log.info("Now do the same filter & subset, but select complement of columns.")
                     filterHex <- hex[!( ( ( hex[,c("PSA")] != 51.2906042845 )) ), c("DPROS","DCAPS","VOL","GLEASON","RACE","ID","AGE")]
            }
            conn = new("H2OClient", ip=myIP, port=myPort)
            tryCatch(test_that("compoundFilterTest_ on data prostate_long", complexFilterTest_prostate_long_134(conn)), warning = function(w) WARN(w), error = function(e) FAIL(e))
            PASS()

            ##
            # Author: Autogenerated on 2013-11-27 03:20:00
            # gitHash: 25eb0cfd1269362ae85d0f073d9abde2a68ac0e4
            # SEED: 4617575514352894151
            ##
            source('./findNSourceUtils.R')
            Log.info("======================== Begin Test ===========================")
            sliceTest_coldom_train_2 <- function(conn) {
                Log.info("A munge-task R unit test on data <coldom_train> testing the functional unit <[> ")
                Log.info("Uploading coldom_train")
                hex <- h2o.uploadFile(conn, "../../smalldata/test/classifier/coldom_train.csv", "rcoldom_train.hex")
                    Log.info("Performing a row slice of coldom_train using these rows: c(1,3,2)")
                slicedHex <- hex[c(1,3,2),]
                    Log.info("Performing a 1-by-1 column slice of coldom_train using these columns: ")
                    Log.info("Performing a 1-by-1 row slice of coldom_train using these rows: ")
            }
            conn = new("H2OClient", ip=myIP, port=myPort)
            tryCatch(test_that("sliceTest_ on data coldom_train", sliceTest_coldom_train_2(conn)), warning = function(w) WARN(w), error = function(e) FAIL(e))
            PASS()

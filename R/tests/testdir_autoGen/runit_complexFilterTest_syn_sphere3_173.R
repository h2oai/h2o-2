            ##
            # Author: Autogenerated on 2013-11-27 03:20:00
            # gitHash: 25eb0cfd1269362ae85d0f073d9abde2a68ac0e4
            # SEED: 4617575514352894151
            ##
            source('./findNSourceUtils.R')
            Log.info("======================== Begin Test ===========================")
            complexFilterTest_syn_sphere3_173 <- function(conn) {
                Log.info("A munge-task R unit test on data <syn_sphere3> testing the functional unit <['!', '!=', '|', '<', '|', '>=']> ")
                Log.info("Uploading syn_sphere3")
                hex <- h2o.uploadFile(conn, "../../smalldata/syn_sphere3.csv", "rsyn_sphere3.hex")
            Log.info("Performing compound task !( ( hex[,c(1)] != 11.8203040851 ) | ( hex[,c(2)] < 59.5077871601 )) on dataset <syn_sphere3>")
                     filterHex <- hex[!( ( hex[,c(1)] != 11.8203040851 ) | ( hex[,c(2)] < 59.5077871601 )),]
            Log.info("Performing compound task !( ( hex[,c(1)] != 13.3633094021 ) | ( hex[,c(1)] < 19.471944918 )) on dataset syn_sphere3, and also subsetting columns.")
                     filterHex <- hex[!( ( hex[,c(1)] != 13.3633094021 ) | ( hex[,c(1)] < 19.471944918 )), c(1)]
                Log.info("Now do the same filter & subset, but select complement of columns.")
                     filterHex <- hex[!( ( hex[,c(1)] != 13.3633094021 ) | ( hex[,c(1)] < 19.471944918 )), c(2)]
            }
            conn = new("H2OClient", ip=myIP, port=myPort)
            tryCatch(test_that("compoundFilterTest_ on data syn_sphere3", complexFilterTest_syn_sphere3_173(conn)), warning = function(w) WARN(w), error = function(e) FAIL(e))
            PASS()

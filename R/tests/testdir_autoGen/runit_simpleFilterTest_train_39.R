            ##
            # Author: Autogenerated on 2013-11-27 18:13:58
            # gitHash: c4ad841105ba82f4a3979e4cf1ae7e20a5905e59
            # SEED: 4663640625336856642
            ##
            source('./findNSourceUtils.R')
            Log.info("======================== Begin Test ===========================")
            simpleFilterTest_train_39 <- function(conn) {
                Log.info("A munge-task R unit test on data <train> testing the functional unit <<> ")
                Log.info("Uploading train")
                hex <- h2o.uploadFile(conn, locate("../../smalldata/chess/chess_2x2x500/R/train.csv"), "rtrain.hex")
                Log.info("Filtering out rows by < from dataset train and column \"x\" using value 0.126349691726")
                     filterHex <- hex[hex[,c("x")] < 0.126349691726,]
                    Log.info("Perform filtering with the '$' sign also")
                    filterHex <- hex[hex$"x" < 0.126349691726,]
                Log.info("Filtering out rows by < from dataset train and column \"x\" using value 1.50867919726")
                     filterHex <- hex[hex[,c("x")] < 1.50867919726,]
                    Log.info("Perform filtering with the '$' sign also")
                    filterHex <- hex[hex$"x" < 1.50867919726,]
                Log.info("Filtering out rows by < from dataset train and column \"y\" using value 1.28201972978")
                     filterHex <- hex[hex[,c("y")] < 1.28201972978,]
                    Log.info("Perform filtering with the '$' sign also")
                    filterHex <- hex[hex$"y" < 1.28201972978,]
                    Log.info("Filtering out rows by < from dataset train and column \"y\" using value 0.405831611242, and also subsetting columns.")
                     filterHex <- hex[hex[,c("y")] < 0.405831611242, c("y")]
                    Log.info("Now do the same filter & subset, but select complement of columns.")
                     filterHex <- hex[hex[,c("y")] < 0.405831611242, c("color","x","y")]
                    Log.info("Filtering out rows by < from dataset train and column \"y\" using value 0.317378372906, and also subsetting columns.")
                     filterHex <- hex[hex[,c("y")] < 0.317378372906, c("y")]
                    Log.info("Now do the same filter & subset, but select complement of columns.")
                     filterHex <- hex[hex[,c("y")] < 0.317378372906, c("color","x","y")]
                    Log.info("Filtering out rows by < from dataset train and column \"x\" using value 1.79011501993, and also subsetting columns.")
                     filterHex <- hex[hex[,c("x")] < 1.79011501993, c("x")]
                    Log.info("Now do the same filter & subset, but select complement of columns.")
                     filterHex <- hex[hex[,c("x")] < 1.79011501993, c("color","x","y")]
            }
            conn = new("H2OClient", ip=myIP, port=myPort)
            tryCatch(test_that("simpleFilterTest_ on data train", simpleFilterTest_train_39(conn)), warning = function(w) WARN(w), error = function(e) FAIL(e))
            PASS()

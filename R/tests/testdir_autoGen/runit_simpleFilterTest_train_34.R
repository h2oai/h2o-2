            ##
            # Author: Autogenerated on 2013-11-27 18:13:58
            # gitHash: c4ad841105ba82f4a3979e4cf1ae7e20a5905e59
            # SEED: 4663640625336856642
            ##
            source('./findNSourceUtils.R')
            Log.info("======================== Begin Test ===========================")
            simpleFilterTest_train_34 <- function(conn) {
                Log.info("A munge-task R unit test on data <train> testing the functional unit <<> ")
                Log.info("Uploading train")
                hex <- h2o.uploadFile(conn, locate("../../smalldata/chess/chess_2x2x1000/weka/train.csv.arff"), "rtrain.hex")
                Log.info("Filtering out rows by < from dataset train and column \"1\" using value 1.15431175589")
                     filterHex <- hex[hex[,c(1)] < 1.15431175589,]
                    Log.info("Perform filtering with the '$' sign also")
                    filterHex <- hex[hex$"1" < 1.15431175589,]
                Log.info("Filtering out rows by < from dataset train and column \"0\" using value 0.854388007239")
                     filterHex <- hex[hex[,c(1)] < 0.854388007239,]
                    Log.info("Perform filtering with the '$' sign also")
                    filterHex <- hex[hex$"0" < 0.854388007239,]
                Log.info("Filtering out rows by < from dataset train and column \"1\" using value 0.704345651628")
                     filterHex <- hex[hex[,c(1)] < 0.704345651628,]
                    Log.info("Perform filtering with the '$' sign also")
                    filterHex <- hex[hex$"1" < 0.704345651628,]
                    Log.info("Filtering out rows by < from dataset train and column \"1\" using value 1.3394066111, and also subsetting columns.")
                     filterHex <- hex[hex[,c(1)] < 1.3394066111, c(1)]
                    Log.info("Now do the same filter & subset, but select complement of columns.")
                     filterHex <- hex[hex[,c(1)] < 1.3394066111, c(1,2)]
                    Log.info("Filtering out rows by < from dataset train and column \"0\" using value 1.67126249071, and also subsetting columns.")
                     filterHex <- hex[hex[,c(1)] < 1.67126249071, c(1)]
                    Log.info("Now do the same filter & subset, but select complement of columns.")
                     filterHex <- hex[hex[,c(1)] < 1.67126249071, c(1,2)]
                    Log.info("Filtering out rows by < from dataset train and column \"1\" using value 1.25086527109, and also subsetting columns.")
                     filterHex <- hex[hex[,c(1)] < 1.25086527109, c(1)]
                    Log.info("Now do the same filter & subset, but select complement of columns.")
                     filterHex <- hex[hex[,c(1)] < 1.25086527109, c(1,2)]
            }
            conn = new("H2OClient", ip=myIP, port=myPort)
            tryCatch(test_that("simpleFilterTest_ on data train", simpleFilterTest_train_34(conn)), warning = function(w) WARN(w), error = function(e) FAIL(e))
            PASS()

            ##
            # Author: Autogenerated on 2013-11-27 18:13:58
            # gitHash: c4ad841105ba82f4a3979e4cf1ae7e20a5905e59
            # SEED: 4663640625336856642
            ##
            source('./findNSourceUtils.R')
            Log.info("======================== Begin Test ===========================")
            sliceTest_iris_train_numeric_5 <- function(conn) {
                Log.info("A munge-task R unit test on data <iris_train_numeric> testing the functional unit <[> ")
                Log.info("Uploading iris_train_numeric")
                hex <- h2o.uploadFile(conn, locate("../../smalldata/test/classifier/iris_train_numeric.csv"), "riris_train_numeric.hex")
                Log.info("Performing a column slice of iris_train_numeric using these columns: c(\"sepal_wid\",\"sepal_len\")")
                slicedHex <- hex[,c("sepal_wid","sepal_len")]
                    Log.info("Performing a row slice of iris_train_numeric using these rows: c(1,3,2,5,4,6)")
                slicedHex <- hex[c(1,3,2,5,4,6),]
                    Log.info("Performing a 1-by-1 column slice of iris_train_numeric using these columns: ")
                    Log.info("Performing a 1-by-1 row slice of iris_train_numeric using these rows: ")
            }
            conn = new("H2OClient", ip=myIP, port=myPort)
            tryCatch(test_that("sliceTest_ on data iris_train_numeric", sliceTest_iris_train_numeric_5(conn)), warning = function(w) WARN(w), error = function(e) FAIL(e))
            PASS()

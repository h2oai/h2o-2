            ##
            # Author: Autogenerated on 2013-11-27 17:26:17
            # gitHash: a4fe16d99eb5f003dd7c7d0da17f29e1712eda0f
            # SEED: 8586424275721420395
            ##
            source('./findNSourceUtils.R')
            Log.info("======================== Begin Test ===========================")
            complexFilterTest_poker10_137 <- function(conn) {
                Log.info("A munge-task R unit test on data <poker10> testing the functional unit <['!', '!=']> ")
                Log.info("Uploading poker10")
                hex <- h2o.uploadFile(conn, locate("../../smalldata/poker/poker10"), "rpoker10.hex")
            Log.info("Performing compound task !( ( ( hex[,c(7)] != 4.0 )) ) on dataset <poker10>")
                     filterHex <- hex[!( ( ( hex[,c(7)] != 4.0 )) ),]
            Log.info("Performing compound task !( ( ( hex[,c(6)] != 1.0 )) ) on dataset poker10, and also subsetting columns.")
                     filterHex <- hex[!( ( ( hex[,c(6)] != 1.0 )) ), c(10,2,5,4,7,6,9,8)]
                Log.info("Now do the same filter & subset, but select complement of columns.")
                     filterHex <- hex[!( ( ( hex[,c(6)] != 1.0 )) ), c(1,3)]
            }
            conn = new("H2OClient", ip=myIP, port=myPort)
            tryCatch(test_that("compoundFilterTest_ on data poker10", complexFilterTest_poker10_137(conn)), warning = function(w) WARN(w), error = function(e) FAIL(e))
            PASS()

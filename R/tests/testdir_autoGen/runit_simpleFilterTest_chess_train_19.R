            ##
            # Author: Autogenerated on 2013-11-29 01:20:08
            # gitHash: 10810480518d43dd720690e729d2f3b9a0f8eba7
            # SEED: 7977475956795433203
            ##
            source('./findNSourceUtils.R')
            Log.info("======================== Begin Test ===========================")
            simpleFilterTest_chess_train_19 <- function(conn) {
                Log.info("A munge-task R unit test on data <chess_train> testing the functional unit <<> ")
                Log.info("Uploading chess_train")
                hex <- h2o.uploadFile(conn, locate("../../smalldata/test/classifier/chess_train.csv"), "rchess_train.hex")
                Log.info("Filtering out rows by < from dataset chess_train and column \"x\" using value 0.720186719398")
                     filterHex <- hex[hex[,c("x")] < 0.720186719398,]
                    Log.info("Perform filtering with the '$' sign also")
                    filterHex <- hex[hex$"x" < 0.720186719398,]
                Log.info("Filtering out rows by < from dataset chess_train and column \"y\" using value 1.89325066119")
                     filterHex <- hex[hex[,c("y")] < 1.89325066119,]
                    Log.info("Perform filtering with the '$' sign also")
                    filterHex <- hex[hex$"y" < 1.89325066119,]
                    Log.info("Filtering out rows by < from dataset chess_train and column \"x\" using value 0.971107888475, and also subsetting columns.")
                     filterHex <- hex[hex[,c("x")] < 0.971107888475, c("x")]
                    Log.info("Now do the same filter & subset, but select complement of columns.")
                     filterHex <- hex[hex[,c("x")] < 0.971107888475, c("color","x","y")]
                    Log.info("Filtering out rows by < from dataset chess_train and column \"x\" using value 0.529742307305, and also subsetting columns.")
                     filterHex <- hex[hex[,c("x")] < 0.529742307305, c("x")]
                    Log.info("Now do the same filter & subset, but select complement of columns.")
                     filterHex <- hex[hex[,c("x")] < 0.529742307305, c("color","x","y")]
            Log.info("End of test")
            PASSS <<- TRUE
            }
            PASSS <- FALSE
            conn = new("H2OClient", ip=myIP, port=myPort)
            tryCatch(test_that("simpleFilterTest_ on data chess_train", simpleFilterTest_chess_train_19(conn)), warning = function(w) WARN(w), error = function(e) FAIL(e))
            if (!PASSS) FAIL("Did not reach the end of test. Check Rsandbox/errors.log for warnings and errors.")
            PASS()

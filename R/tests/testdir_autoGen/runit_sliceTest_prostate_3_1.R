            ##
            # Author: Autogenerated on 2013-11-29 01:20:07
            # gitHash: 10810480518d43dd720690e729d2f3b9a0f8eba7
            # SEED: 7977475956795433203
            ##
            source('./findNSourceUtils.R')
            Log.info("======================== Begin Test ===========================")
            sliceTest_prostate_3_1 <- function(conn) {
                Log.info("A munge-task R unit test on data <prostate_3> testing the functional unit <[> ")
                Log.info("Uploading prostate_3")
                hex <- h2o.uploadFile(conn, locate("../../smalldata/parse_folder_test/prostate_3.csv"), "rprostate_3.hex")
                    Log.info("Performing a row slice of prostate_3 using these rows: c(20,21,22,23,1,3,2,5,4,7,6,9,8,11,10,13,12,15,14,17,16,19,18)")
                slicedHex <- hex[c(20,21,22,23,1,3,2,5,4,7,6,9,8,11,10,13,12,15,14,17,16,19,18),]
                    Log.info("Performing a row & column slice of prostate_3 using these rows & columns: c(1,5,7,6) & c(38,20,48,33,47,1,5,4)")
                slicedHex <- hex[c(38,20,48,33,47,1,5,4),c(1,5,7,6)]
                    Log.info("Performing a 1-by-1 column slice of prostate_3 using these columns: ")
                    Log.info("Performing a 1-by-1 row slice of prostate_3 using these rows: ")
            Log.info("End of test")
            PASSS <<- TRUE
            }
            PASSS <- FALSE
            conn = new("H2OClient", ip=myIP, port=myPort)
            tryCatch(test_that("sliceTest_ on data prostate_3", sliceTest_prostate_3_1(conn)), warning = function(w) WARN(w), error = function(e) FAIL(e))
            if (!PASSS) FAIL("Did not reach the end of test. Check Rsandbox/errors.log for warnings and errors.")
            PASS()

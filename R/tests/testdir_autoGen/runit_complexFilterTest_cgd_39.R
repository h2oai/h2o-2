            ##
            # Author: Autogenerated on 2013-12-05 22:49:50
            # gitHash: dbc23230f6666890a6cb7c4234e68e540e096e0c
            # SEED: 779905237530228736
            ##
            source('./findNSourceUtils.R')
            complexFilterTest_cgd_39 <- function(conn) {
                Log.info("A munge-task R unit test on data <cgd> testing the functional unit <['', '>=']> ")
                Log.info("Uploading cgd")
                hex <- h2o.uploadFile(conn, locate("../../smalldata/logreg/umass_statdata/cgd.dat"), "rcgd.hex")
            Log.info("Performing compound task ( ( hex[,c(5)] >= 34.5572327189 ))  on dataset <cgd>")
                     filterHex <- hex[( ( hex[,c(5)] >= 34.5572327189 )) ,]
            Log.info("Performing compound task ( ( hex[,c(3)] >= 1.05639189931 ))  on dataset cgd, and also subsetting columns.")
                     filterHex <- hex[( ( hex[,c(3)] >= 1.05639189931 )) , c(11,10,13,12,15,1,3,2,9)]
                Log.info("Now do the same filter & subset, but select complement of columns.")
                     filterHex <- hex[( ( hex[,c(3)] >= 1.05639189931 )) , c(14,5,4,7,6,8)]
            testEnd()
            }
            doTest("compoundFilterTest_ on data cgd unit= ['', '>=']", complexFilterTest_cgd_39)

            ##
            # Author: Autogenerated on 2013-11-27 17:26:17
            # gitHash: a4fe16d99eb5f003dd7c7d0da17f29e1712eda0f
            # SEED: 8586424275721420395
            ##
            source('./findNSourceUtils.R')
            Log.info("======================== Begin Test ===========================")
            complexFilterTest_USArrests_169 <- function(conn) {
                Log.info("A munge-task R unit test on data <USArrests> testing the functional unit <['!', '==', '&', '>', '|', '>=']> ")
                Log.info("Uploading USArrests")
                hex <- h2o.uploadFile(conn, locate("../../smalldata/pca_test/USArrests.csv"), "rUSArrests.hex")
            Log.info("Performing compound task !( ( hex[,c(\"UrbanPop\")] == 82.0294770116 ) & ( hex[,c(\"Rape\")] > 10.9077683601 ) | ( ( hex[,c(\"UrbanPop\")] >= 83.6739673597 )) ) on dataset <USArrests>")
                     filterHex <- hex[!( ( hex[,c("UrbanPop")] == 82.0294770116 ) & ( hex[,c("Rape")] > 10.9077683601 ) | ( ( hex[,c("UrbanPop")] >= 83.6739673597 )) ),]
            Log.info("Performing compound task !( ( hex[,c(\"Rape\")] == 36.6331222484 ) & ( hex[,c(\"UrbanPop\")] > 57.0400212676 ) | ( ( hex[,c(\"Rape\")] >= 40.7574731669 )) ) on dataset USArrests, and also subsetting columns.")
                     filterHex <- hex[!( ( hex[,c("Rape")] == 36.6331222484 ) & ( hex[,c("UrbanPop")] > 57.0400212676 ) | ( ( hex[,c("Rape")] >= 40.7574731669 )) ), c("UrbanPop","Assault","Rape","Murder")]
                Log.info("Now do the same filter & subset, but select complement of columns.")
                     filterHex <- hex[!( ( hex[,c("Rape")] == 36.6331222484 ) & ( hex[,c("UrbanPop")] > 57.0400212676 ) | ( ( hex[,c("Rape")] >= 40.7574731669 )) ), c(1)]
            }
            conn = new("H2OClient", ip=myIP, port=myPort)
            tryCatch(test_that("compoundFilterTest_ on data USArrests", complexFilterTest_USArrests_169(conn)), warning = function(w) WARN(w), error = function(e) FAIL(e))
            PASS()

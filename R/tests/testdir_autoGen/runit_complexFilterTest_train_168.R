            ##
            # Author: Autogenerated on 2013-11-27 18:13:59
            # gitHash: c4ad841105ba82f4a3979e4cf1ae7e20a5905e59
            # SEED: 4663640625336856642
            ##
            source('./findNSourceUtils.R')
            Log.info("======================== Begin Test ===========================")
            complexFilterTest_train_168 <- function(conn) {
                Log.info("A munge-task R unit test on data <train> testing the functional unit <['!', '>', '&', '==', '&', '<=']> ")
                Log.info("Uploading train")
                hex <- h2o.uploadFile(conn, locate("../../smalldata/chess/chess_2x2x10/h2o/train.csv"), "rtrain.hex")
            Log.info("Performing compound task !( ( hex[,c(\"x\")] > 0.508382551442 ) & ( hex[,c(\"y\")] == 1.37785029268 ) & ( ( hex[,c(\"y\")] <= 0.726190713813 )) ) on dataset <train>")
                     filterHex <- hex[!( ( hex[,c("x")] > 0.508382551442 ) & ( hex[,c("y")] == 1.37785029268 ) & ( ( hex[,c("y")] <= 0.726190713813 )) ),]
            Log.info("Performing compound task !( ( hex[,c(\"y\")] > 0.594479977618 ) & ( hex[,c(\"x\")] == 0.115989383534 ) & ( ( hex[,c(\"x\")] <= 1.53179371568 )) ) on dataset train, and also subsetting columns.")
                     filterHex <- hex[!( ( hex[,c("y")] > 0.594479977618 ) & ( hex[,c("x")] == 0.115989383534 ) & ( ( hex[,c("x")] <= 1.53179371568 )) ), c("x","y")]
                Log.info("Now do the same filter & subset, but select complement of columns.")
                     filterHex <- hex[!( ( hex[,c("y")] > 0.594479977618 ) & ( hex[,c("x")] == 0.115989383534 ) & ( ( hex[,c("x")] <= 1.53179371568 )) ), c("color")]
            }
            conn = new("H2OClient", ip=myIP, port=myPort)
            tryCatch(test_that("compoundFilterTest_ on data train", complexFilterTest_train_168(conn)), warning = function(w) WARN(w), error = function(e) FAIL(e))
            PASS()

            ##
            # Author: Autogenerated on 2013-11-27 17:26:17
            # gitHash: a4fe16d99eb5f003dd7c7d0da17f29e1712eda0f
            # SEED: 8586424275721420395
            ##
            source('./findNSourceUtils.R')
            Log.info("======================== Begin Test ===========================")
            complexFilterTest_prostate_8_142 <- function(conn) {
                Log.info("A munge-task R unit test on data <prostate_8> testing the functional unit <['!', '!=']> ")
                Log.info("Uploading prostate_8")
                hex <- h2o.uploadFile(conn, locate("../../smalldata/parse_folder_test/prostate_8.csv"), "rprostate_8.hex")
            Log.info("Performing compound task !( ( ( hex[,c(5)] != 1.15776908663 )) ) on dataset <prostate_8>")
                     filterHex <- hex[!( ( ( hex[,c(5)] != 1.15776908663 )) ),]
            Log.info("Performing compound task !( ( ( hex[,c(8)] != 7.19436666487 )) ) on dataset prostate_8, and also subsetting columns.")
                     filterHex <- hex[!( ( ( hex[,c(8)] != 7.19436666487 )) ), c(1,2,5,4,7,6,8)]
                Log.info("Now do the same filter & subset, but select complement of columns.")
                     filterHex <- hex[!( ( ( hex[,c(8)] != 7.19436666487 )) ), c(3)]
            }
            conn = new("H2OClient", ip=myIP, port=myPort)
            tryCatch(test_that("compoundFilterTest_ on data prostate_8", complexFilterTest_prostate_8_142(conn)), warning = function(w) WARN(w), error = function(e) FAIL(e))
            PASS()

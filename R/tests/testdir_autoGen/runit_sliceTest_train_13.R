            ##
            # Author: Autogenerated on 2013-11-27 03:20:00
            # gitHash: 25eb0cfd1269362ae85d0f073d9abde2a68ac0e4
            # SEED: 4617575514352894151
            ##
            source('./findNSourceUtils.R')
            Log.info("======================== Begin Test ===========================")
            sliceTest_train_13 <- function(conn) {
                Log.info("A munge-task R unit test on data <train> testing the functional unit <[> ")
                Log.info("Uploading train")
                hex <- h2o.uploadFile(conn, "../../smalldata/chess/chess_2x2x10/weka/train.csv.arff", "rtrain.hex")
                Log.info("Performing a column slice of train using these columns: c(1)")
                slicedHex <- hex[,c(1)]
                    Log.info("Performing a row & column slice of train using these rows & columns: c(2) & c(24,11,26,12,15,23,2,5,25)")
                slicedHex <- hex[c(24,11,26,12,15,23,2,5,25),c(2)]
                    Log.info("Performing a 1-by-1 column slice of train using these columns: ")
                    Log.info("Performing a 1-by-1 row slice of train using these rows: ")
            }
            conn = new("H2OClient", ip=myIP, port=myPort)
            tryCatch(test_that("sliceTest_ on data train", sliceTest_train_13(conn)), warning = function(w) WARN(w), error = function(e) FAIL(e))
            PASS()

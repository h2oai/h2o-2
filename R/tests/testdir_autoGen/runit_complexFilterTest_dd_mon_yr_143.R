            ##
            # Author: Autogenerated on 2013-11-27 03:20:00
            # gitHash: 25eb0cfd1269362ae85d0f073d9abde2a68ac0e4
            # SEED: 4617575514352894151
            ##
            source('./findNSourceUtils.R')
            Log.info("======================== Begin Test ===========================")
            complexFilterTest_dd_mon_yr_143 <- function(conn) {
                Log.info("A munge-task R unit test on data <dd-mon-yr> testing the functional unit <['!', '>=']> ")
                Log.info("Uploading dd-mon-yr")
                hex <- h2o.uploadFile(conn, "../../smalldata/datetime/dd-mon-yr.csv", "rdd-mon-yr.hex")
            Log.info("Performing compound task !( ( ( hex[,c(1)] >= 1.35166847918e+12 )) ) on dataset <dd-mon-yr>")
                     filterHex <- hex[!( ( ( hex[,c(1)] >= 1.35166847918e+12 )) ),]
            Log.info("Performing compound task !( ( ( hex[,c(1)] >= 1.3516355455e+12 )) ) on dataset dd-mon-yr, and also subsetting columns.")
                     filterHex <- hex[!( ( ( hex[,c(1)] >= 1.3516355455e+12 )) ), c(1)]
                Log.info("Now do the same filter & subset, but select complement of columns.")
                     filterHex <- hex[!( ( ( hex[,c(1)] >= 1.3516355455e+12 )) ), c(1)]
            }
            conn = new("H2OClient", ip=myIP, port=myPort)
            tryCatch(test_that("compoundFilterTest_ on data dd-mon-yr", complexFilterTest_dd_mon_yr_143(conn)), warning = function(w) WARN(w), error = function(e) FAIL(e))
            PASS()

            ##
            # Author: Autogenerated on 2013-11-27 03:20:00
            # gitHash: 25eb0cfd1269362ae85d0f073d9abde2a68ac0e4
            # SEED: 4617575514352894151
            ##
            source('./findNSourceUtils.R')
            Log.info("======================== Begin Test ===========================")
            complexFilterTest_train_137 <- function(conn) {
                Log.info("A munge-task R unit test on data <train> testing the functional unit <['!', '>=']> ")
                Log.info("Uploading train")
                hex <- h2o.uploadFile(conn, "../../smalldata/chess/chess_2x2x200/weka/train.csv.arff", "rtrain.hex")
            Log.info("Performing compound task !( ( ( hex[,c(1)] >= 1.49056146025 )) ) on dataset <train>")
                     filterHex <- hex[!( ( ( hex[,c(1)] >= 1.49056146025 )) ),]
            Log.info("Performing compound task !( ( ( hex[,c(1)] >= 1.54354790626 )) ) on dataset train, and also subsetting columns.")
                     filterHex <- hex[!( ( ( hex[,c(1)] >= 1.54354790626 )) ), c(1)]
                Log.info("Now do the same filter & subset, but select complement of columns.")
                     filterHex <- hex[!( ( ( hex[,c(1)] >= 1.54354790626 )) ), c(2)]
            }
            conn = new("H2OClient", ip=myIP, port=myPort)
            tryCatch(test_that("compoundFilterTest_ on data train", complexFilterTest_train_137(conn)), warning = function(w) WARN(w), error = function(e) FAIL(e))
            PASS()

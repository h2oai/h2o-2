            ##
            # Author: Autogenerated on 2013-11-27 18:13:58
            # gitHash: c4ad841105ba82f4a3979e4cf1ae7e20a5905e59
            # SEED: 4663640625336856642
            ##
            source('./findNSourceUtils.R')
            Log.info("======================== Begin Test ===========================")
            sliceTest_allyears2k_8 <- function(conn) {
                Log.info("A munge-task R unit test on data <allyears2k> testing the functional unit <[> ")
                Log.info("Uploading allyears2k")
                hex <- h2o.uploadFile(conn, locate("../../smalldata/airlines/allyears2k.zip"), "rallyears2k.hex")
                Log.info("Performing a column slice of allyears2k using these columns: c(1,3,2,5,4)")
                slicedHex <- hex[,c(1,3,2,5,4)]
                    Log.info("Performing a 1-by-1 column slice of allyears2k using these columns: ")
                    Log.info("Performing a 1-by-1 row slice of allyears2k using these rows: ")
            }
            conn = new("H2OClient", ip=myIP, port=myPort)
            tryCatch(test_that("sliceTest_ on data allyears2k", sliceTest_allyears2k_8(conn)), warning = function(w) WARN(w), error = function(e) FAIL(e))
            PASS()

            ##
            # Author: Autogenerated on 2013-11-29 01:20:08
            # gitHash: 10810480518d43dd720690e729d2f3b9a0f8eba7
            # SEED: 7977475956795433203
            ##
            source('./findNSourceUtils.R')
            Log.info("======================== Begin Test ===========================")
            simpleFilterTest_coldom_test_67 <- function(conn) {
                Log.info("A munge-task R unit test on data <coldom_test> testing the functional unit <!=> ")
                Log.info("Uploading coldom_test")
                hex <- h2o.uploadFile(conn, locate("../../smalldata/test/classifier/coldom_test.csv"), "rcoldom_test.hex")
                Log.info("Filtering out rows by != from dataset coldom_test and column \"4\" using value 0.68172972624")
                     filterHex <- hex[hex[,c(4)] != 0.68172972624,]
                    Log.info("Perform filtering with the '$' sign also")
                    filterHex <- hex[hex$"4" != 0.68172972624,]
                Log.info("Filtering out rows by != from dataset coldom_test and column \"4\" using value 0.850846102259")
                     filterHex <- hex[hex[,c(4)] != 0.850846102259,]
                    Log.info("Perform filtering with the '$' sign also")
                    filterHex <- hex[hex$"4" != 0.850846102259,]
                    Log.info("Filtering out rows by != from dataset coldom_test and column \"4\" using value 0.482293078103, and also subsetting columns.")
                     filterHex <- hex[hex[,c(4)] != 0.482293078103, c(4)]
                    Log.info("Now do the same filter & subset, but select complement of columns.")
                     filterHex <- hex[hex[,c(4)] != 0.482293078103, c(1,3,2,4)]
                    Log.info("Filtering out rows by != from dataset coldom_test and column \"4\" using value 0.948624440856, and also subsetting columns.")
                     filterHex <- hex[hex[,c(4)] != 0.948624440856, c(4)]
                    Log.info("Now do the same filter & subset, but select complement of columns.")
                     filterHex <- hex[hex[,c(4)] != 0.948624440856, c(1,3,2,4)]
            Log.info("End of test")
            PASSS <<- TRUE
            }
            PASSS <- FALSE
            conn = new("H2OClient", ip=myIP, port=myPort)
            tryCatch(test_that("simpleFilterTest_ on data coldom_test", simpleFilterTest_coldom_test_67(conn)), warning = function(w) WARN(w), error = function(e) FAIL(e))
            if (!PASSS) FAIL("Did not reach the end of test. Check Rsandbox/errors.log for warnings and errors.")
            PASS()

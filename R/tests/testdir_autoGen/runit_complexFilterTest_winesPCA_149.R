            ##
            # Author: Autogenerated on 2013-11-27 17:26:17
            # gitHash: a4fe16d99eb5f003dd7c7d0da17f29e1712eda0f
            # SEED: 8586424275721420395
            ##
            source('./findNSourceUtils.R')
            Log.info("======================== Begin Test ===========================")
            complexFilterTest_winesPCA_149 <- function(conn) {
                Log.info("A munge-task R unit test on data <winesPCA> testing the functional unit <['!', '!=']> ")
                Log.info("Uploading winesPCA")
                hex <- h2o.uploadFile(conn, locate("../../smalldata/winesPCA.csv"), "rwinesPCA.hex")
            Log.info("Performing compound task !( ( ( hex[,c(1)] != 1.52725314883 )) ) on dataset <winesPCA>")
                     filterHex <- hex[!( ( ( hex[,c(1)] != 1.52725314883 )) ),]
            Log.info("Performing compound task !( ( ( hex[,c(1)] != 1.69120264295 )) ) on dataset winesPCA, and also subsetting columns.")
                     filterHex <- hex[!( ( ( hex[,c(1)] != 1.69120264295 )) ), c(1)]
                Log.info("Now do the same filter & subset, but select complement of columns.")
                     filterHex <- hex[!( ( ( hex[,c(1)] != 1.69120264295 )) ), c(1)]
            }
            conn = new("H2OClient", ip=myIP, port=myPort)
            tryCatch(test_that("compoundFilterTest_ on data winesPCA", complexFilterTest_winesPCA_149(conn)), warning = function(w) WARN(w), error = function(e) FAIL(e))
            PASS()

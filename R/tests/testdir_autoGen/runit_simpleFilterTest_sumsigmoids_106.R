            ##
            # Author: Autogenerated on 2013-11-27 18:13:59
            # gitHash: c4ad841105ba82f4a3979e4cf1ae7e20a5905e59
            # SEED: 4663640625336856642
            ##
            source('./findNSourceUtils.R')
            Log.info("======================== Begin Test ===========================")
            simpleFilterTest_sumsigmoids_106 <- function(conn) {
                Log.info("A munge-task R unit test on data <sumsigmoids> testing the functional unit <==> ")
                Log.info("Uploading sumsigmoids")
                hex <- h2o.uploadFile(conn, locate("../../smalldata/neural/sumsigmoids.csv"), "rsumsigmoids.hex")
                Log.info("Filtering out rows by == from dataset sumsigmoids and column \"Y\" using value -0.0435691471268")
                     filterHex <- hex[hex[,c("Y")] == -0.0435691471268,]
                    Log.info("Perform filtering with the '$' sign also")
                    filterHex <- hex[hex$"Y" == -0.0435691471268,]
                    Log.info("Filtering out rows by == from dataset sumsigmoids and column \"X.1\" using value -0.919345558526, and also subsetting columns.")
                     filterHex <- hex[hex[,c("X.1")] == -0.919345558526, c("X.1")]
                    Log.info("Now do the same filter & subset, but select complement of columns.")
                     filterHex <- hex[hex[,c("X.1")] == -0.919345558526, c("X.1","Y","X.2")]
            }
            conn = new("H2OClient", ip=myIP, port=myPort)
            tryCatch(test_that("simpleFilterTest_ on data sumsigmoids", simpleFilterTest_sumsigmoids_106(conn)), warning = function(w) WARN(w), error = function(e) FAIL(e))
            PASS()

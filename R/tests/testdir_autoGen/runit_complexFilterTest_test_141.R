            ##
            # Author: Autogenerated on 2013-11-27 17:26:17
            # gitHash: a4fe16d99eb5f003dd7c7d0da17f29e1712eda0f
            # SEED: 8586424275721420395
            ##
            source('./findNSourceUtils.R')
            Log.info("======================== Begin Test ===========================")
            complexFilterTest_test_141 <- function(conn) {
                Log.info("A munge-task R unit test on data <test> testing the functional unit <['!', '!=']> ")
                Log.info("Uploading test")
                hex <- h2o.uploadFile(conn, locate("../../smalldata/chess/chess_2x2x500/h2o/test.csv"), "rtest.hex")
            Log.info("Performing compound task !( ( ( hex[,c(\"x\")] != 0.374415321932 )) ) on dataset <test>")
                     filterHex <- hex[!( ( ( hex[,c("x")] != 0.374415321932 )) ),]
            Log.info("Performing compound task !( ( ( hex[,c(\"y\")] != 1.88148658997 )) ) on dataset test, and also subsetting columns.")
                     filterHex <- hex[!( ( ( hex[,c("y")] != 1.88148658997 )) ), c("x","y")]
                Log.info("Now do the same filter & subset, but select complement of columns.")
                     filterHex <- hex[!( ( ( hex[,c("y")] != 1.88148658997 )) ), c("color")]
            }
            conn = new("H2OClient", ip=myIP, port=myPort)
            tryCatch(test_that("compoundFilterTest_ on data test", complexFilterTest_test_141(conn)), warning = function(w) WARN(w), error = function(e) FAIL(e))
            PASS()

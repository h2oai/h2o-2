            ##
            # Author: Autogenerated on 2013-11-27 03:20:00
            # gitHash: 25eb0cfd1269362ae85d0f073d9abde2a68ac0e4
            # SEED: 4617575514352894151
            ##
            source('./findNSourceUtils.R')
            Log.info("======================== Begin Test ===========================")
            simpleFilterTest_train_92 <- function(conn) {
                Log.info("A munge-task R unit test on data <train> testing the functional unit <>=> ")
                Log.info("Uploading train")
                hex <- h2o.uploadFile(conn, "../../smalldata/chess/chess_2x2x500/weka/train.csv.arff", "rtrain.hex")
                Log.info("Filtering out rows by >= from dataset train and column \"0\" using value 1.11015085512")
                     filterHex <- hex[hex[,c(1)] >= 1.11015085512,]
                    Log.info("Perform filtering with the '$' sign also")
                    filterHex <- hex[hex$"0" >= 1.11015085512,]
                Log.info("Filtering out rows by >= from dataset train and column \"0\" using value 1.67072074359")
                     filterHex <- hex[hex[,c(1)] >= 1.67072074359,]
                    Log.info("Perform filtering with the '$' sign also")
                    filterHex <- hex[hex$"0" >= 1.67072074359,]
                    Log.info("Filtering out rows by >= from dataset train and column \"0\" using value 1.83431893517, and also subsetting columns.")
                     filterHex <- hex[hex[,c(1)] >= 1.83431893517, c(1)]
                    Log.info("Now do the same filter & subset, but select complement of columns.")
                     filterHex <- hex[hex[,c(1)] >= 1.83431893517, c(1,2)]
                    Log.info("Filtering out rows by >= from dataset train and column \"1\" using value 1.4091105821, and also subsetting columns.")
                     filterHex <- hex[hex[,c(1)] >= 1.4091105821, c(1)]
                    Log.info("Now do the same filter & subset, but select complement of columns.")
                     filterHex <- hex[hex[,c(1)] >= 1.4091105821, c(1,2)]
            }
            conn = new("H2OClient", ip=myIP, port=myPort)
            tryCatch(test_that("simpleFilterTest_ on data train", simpleFilterTest_train_92(conn)), warning = function(w) WARN(w), error = function(e) FAIL(e))
            PASS()

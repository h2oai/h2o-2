            ##
            # Author: Autogenerated on 2013-11-27 03:20:00
            # gitHash: 25eb0cfd1269362ae85d0f073d9abde2a68ac0e4
            # SEED: 4617575514352894151
            ##
            source('./findNSourceUtils.R')
            Log.info("======================== Begin Test ===========================")
            complexFilterTest_cuse_149 <- function(conn) {
                Log.info("A munge-task R unit test on data <cuse> testing the functional unit <['!', '>=']> ")
                Log.info("Uploading cuse")
                hex <- h2o.uploadFile(conn, "../../smalldata/logreg/princeton/cuse.dat", "rcuse.hex")
            Log.info("Performing compound task !( ( ( hex[,c(\"notUsing\")] >= 123.189396505 )) ) on dataset <cuse>")
                     filterHex <- hex[!( ( ( hex[,c("notUsing")] >= 123.189396505 )) ),]
            Log.info("Performing compound task !( ( ( hex[,c(\"using\")] >= 27.9302386245 )) ) on dataset cuse, and also subsetting columns.")
                     filterHex <- hex[!( ( ( hex[,c("using")] >= 27.9302386245 )) ), c("notUsing","using")]
                Log.info("Now do the same filter & subset, but select complement of columns.")
                     filterHex <- hex[!( ( ( hex[,c("using")] >= 27.9302386245 )) ), c("wantsMore","education","age")]
            }
            conn = new("H2OClient", ip=myIP, port=myPort)
            tryCatch(test_that("compoundFilterTest_ on data cuse", complexFilterTest_cuse_149(conn)), warning = function(w) WARN(w), error = function(e) FAIL(e))
            PASS()

            ##
            # Author: Autogenerated on 2013-12-05 22:49:50
            # gitHash: dbc23230f6666890a6cb7c4234e68e540e096e0c
            # SEED: 779905237530228736
            ##
            source('./findNSourceUtils.R')
            simpleFilterTest_prostate_28 <- function(conn) {
                Log.info("A munge-task R unit test on data <prostate> testing the functional unit <==> ")
                Log.info("Uploading prostate")
                hex <- h2o.uploadFile(conn, locate("../../smalldata/logreg/prostate.csv"), "rprostate.hex")
                Log.info("Filtering out rows by == from dataset prostate and column \"RACE\" using value 0.607585315311")
                     filterHex <- hex[hex[,c("RACE")] == 0.607585315311,]
                    Log.info("Perform filtering with the '$' sign also")
                    filterHex <- hex[hex$"RACE" == 0.607585315311,]
                Log.info("Filtering out rows by == from dataset prostate and column \"DPROS\" using value 2.20276163047")
                     filterHex <- hex[hex[,c("DPROS")] == 2.20276163047,]
                    Log.info("Perform filtering with the '$' sign also")
                    filterHex <- hex[hex$"DPROS" == 2.20276163047,]
                    Log.info("Filtering out rows by == from dataset prostate and column \"DPROS\" using value 1.71355443172, and also subsetting columns.")
                     filterHex <- hex[hex[,c("DPROS")] == 1.71355443172, c("DPROS")]
                    Log.info("Now do the same filter & subset, but select complement of columns.")
                     filterHex <- hex[hex[,c("DPROS")] == 1.71355443172, c("GLEASON","DPROS","PSA","DCAPS","VOL","CAPSULE","RACE","ID","AGE")]
                    Log.info("Filtering out rows by == from dataset prostate and column \"CAPSULE\" using value 0.805393678056, and also subsetting columns.")
                     filterHex <- hex[hex[,c("CAPSULE")] == 0.805393678056, c("CAPSULE")]
                    Log.info("Now do the same filter & subset, but select complement of columns.")
                     filterHex <- hex[hex[,c("CAPSULE")] == 0.805393678056, c("GLEASON","DPROS","PSA","DCAPS","VOL","CAPSULE","RACE","ID","AGE")]
            testEnd()
            }
            doTest("simpleFilterTest_ on data prostate unit= ==", simpleFilterTest_prostate_28)

            ##
            # Author: Autogenerated on 2013-11-27 03:20:00
            # gitHash: 25eb0cfd1269362ae85d0f073d9abde2a68ac0e4
            # SEED: 4617575514352894151
            ##
            source('./findNSourceUtils.R')
            Log.info("======================== Begin Test ===========================")
            complexFilterTest_iris_wheader_135 <- function(conn) {
                Log.info("A munge-task R unit test on data <iris_wheader> testing the functional unit <['!', '>=']> ")
                Log.info("Uploading iris_wheader")
                hex <- h2o.uploadFile(conn, "../../smalldata/iris/iris_wheader.csv", "riris_wheader.hex")
            Log.info("Performing compound task !( ( ( hex[,c(\"sepal_len\")] >= 5.91323252298 )) ) on dataset <iris_wheader>")
                     filterHex <- hex[!( ( ( hex[,c("sepal_len")] >= 5.91323252298 )) ),]
            Log.info("Performing compound task !( ( ( hex[,c(\"petal_len\")] >= 6.20927452403 )) ) on dataset iris_wheader, and also subsetting columns.")
                     filterHex <- hex[!( ( ( hex[,c("petal_len")] >= 6.20927452403 )) ), c("sepal_wid","petal_len")]
                Log.info("Now do the same filter & subset, but select complement of columns.")
                     filterHex <- hex[!( ( ( hex[,c("petal_len")] >= 6.20927452403 )) ), c("petal_wid","class","sepal_len")]
            }
            conn = new("H2OClient", ip=myIP, port=myPort)
            tryCatch(test_that("compoundFilterTest_ on data iris_wheader", complexFilterTest_iris_wheader_135(conn)), warning = function(w) WARN(w), error = function(e) FAIL(e))
            PASS()

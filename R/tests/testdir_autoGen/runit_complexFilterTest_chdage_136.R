            ##
            # Author: Autogenerated on 2013-11-27 03:20:00
            # gitHash: 25eb0cfd1269362ae85d0f073d9abde2a68ac0e4
            # SEED: 4617575514352894151
            ##
            source('./findNSourceUtils.R')
            Log.info("======================== Begin Test ===========================")
            complexFilterTest_chdage_136 <- function(conn) {
                Log.info("A munge-task R unit test on data <chdage> testing the functional unit <['!', '>=']> ")
                Log.info("Uploading chdage")
                hex <- h2o.uploadFile(conn, "../../smalldata/logreg/umass_statdata/chdage.dat", "rchdage.hex")
            Log.info("Performing compound task !( ( ( hex[,c(\"AGE\")] >= 28.1848733875 )) ) on dataset <chdage>")
                     filterHex <- hex[!( ( ( hex[,c("AGE")] >= 28.1848733875 )) ),]
            Log.info("Performing compound task !( ( ( hex[,c(\"ID\")] >= 2.00707356929 )) ) on dataset chdage, and also subsetting columns.")
                     filterHex <- hex[!( ( ( hex[,c("ID")] >= 2.00707356929 )) ), c("ID","AGE")]
                Log.info("Now do the same filter & subset, but select complement of columns.")
                     filterHex <- hex[!( ( ( hex[,c("ID")] >= 2.00707356929 )) ), c("CHD")]
            }
            conn = new("H2OClient", ip=myIP, port=myPort)
            tryCatch(test_that("compoundFilterTest_ on data chdage", complexFilterTest_chdage_136(conn)), warning = function(w) WARN(w), error = function(e) FAIL(e))
            PASS()

            ##
            # Author: Autogenerated on 2013-11-27 17:26:17
            # gitHash: a4fe16d99eb5f003dd7c7d0da17f29e1712eda0f
            # SEED: 8586424275721420395
            ##
            source('./findNSourceUtils.R')
            Log.info("======================== Begin Test ===========================")
            complexFilterTest_train_155 <- function(conn) {
                Log.info("A munge-task R unit test on data <train> testing the functional unit <['!', '==', '&', '>', '|', '>=']> ")
                Log.info("Uploading train")
                hex <- h2o.uploadFile(conn, locate("../../smalldata/chess/chess_2x2x100/h2o/train.csv"), "rtrain.hex")
            Log.info("Performing compound task !( ( hex[,c(\"y\")] == 1.56546454178 ) & ( hex[,c(\"x\")] > 1.04060993599 ) | ( ( hex[,c(\"x\")] >= 1.92414300475 )) ) on dataset <train>")
                     filterHex <- hex[!( ( hex[,c("y")] == 1.56546454178 ) & ( hex[,c("x")] > 1.04060993599 ) | ( ( hex[,c("x")] >= 1.92414300475 )) ),]
            Log.info("Performing compound task !( ( hex[,c(\"y\")] == 1.58316714199 ) & ( hex[,c(\"y\")] > 0.679278948532 ) | ( ( hex[,c(\"x\")] >= 0.528000891538 )) ) on dataset train, and also subsetting columns.")
                     filterHex <- hex[!( ( hex[,c("y")] == 1.58316714199 ) & ( hex[,c("y")] > 0.679278948532 ) | ( ( hex[,c("x")] >= 0.528000891538 )) ), c("x","y")]
                Log.info("Now do the same filter & subset, but select complement of columns.")
                     filterHex <- hex[!( ( hex[,c("y")] == 1.58316714199 ) & ( hex[,c("y")] > 0.679278948532 ) | ( ( hex[,c("x")] >= 0.528000891538 )) ), c("color")]
            }
            conn = new("H2OClient", ip=myIP, port=myPort)
            tryCatch(test_that("compoundFilterTest_ on data train", complexFilterTest_train_155(conn)), warning = function(w) WARN(w), error = function(e) FAIL(e))
            PASS()

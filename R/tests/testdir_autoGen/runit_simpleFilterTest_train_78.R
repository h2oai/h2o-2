            ##
            # Author: Autogenerated on 2013-11-27 17:26:17
            # gitHash: a4fe16d99eb5f003dd7c7d0da17f29e1712eda0f
            # SEED: 8586424275721420395
            ##
            source('./findNSourceUtils.R')
            Log.info("======================== Begin Test ===========================")
            simpleFilterTest_train_78 <- function(conn) {
                Log.info("A munge-task R unit test on data <train> testing the functional unit <>=> ")
                Log.info("Uploading train")
                hex <- h2o.uploadFile(conn, locate("../../smalldata/chess/chess_2x2x10/weka/train.csv.arff"), "rtrain.hex")
                Log.info("Filtering out rows by >= from dataset train and column \"1\" using value 0.657601881115")
                     filterHex <- hex[hex[,c(1)] >= 0.657601881115,]
                    Log.info("Perform filtering with the '$' sign also")
                    filterHex <- hex[hex$"1" >= 0.657601881115,]
                Log.info("Filtering out rows by >= from dataset train and column \"0\" using value 0.708690276738")
                     filterHex <- hex[hex[,c(1)] >= 0.708690276738,]
                    Log.info("Perform filtering with the '$' sign also")
                    filterHex <- hex[hex$"0" >= 0.708690276738,]
                    Log.info("Filtering out rows by >= from dataset train and column \"1\" using value 0.589843838045, and also subsetting columns.")
                     filterHex <- hex[hex[,c(1)] >= 0.589843838045, c(1)]
                    Log.info("Now do the same filter & subset, but select complement of columns.")
                     filterHex <- hex[hex[,c(1)] >= 0.589843838045, c(1,2)]
                    Log.info("Filtering out rows by >= from dataset train and column \"1\" using value 0.289807883766, and also subsetting columns.")
                     filterHex <- hex[hex[,c(1)] >= 0.289807883766, c(1)]
                    Log.info("Now do the same filter & subset, but select complement of columns.")
                     filterHex <- hex[hex[,c(1)] >= 0.289807883766, c(1,2)]
            }
            conn = new("H2OClient", ip=myIP, port=myPort)
            tryCatch(test_that("simpleFilterTest_ on data train", simpleFilterTest_train_78(conn)), warning = function(w) WARN(w), error = function(e) FAIL(e))
            PASS()

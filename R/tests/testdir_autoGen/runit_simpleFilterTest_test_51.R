            ##
            # Author: Autogenerated on 2013-11-29 01:20:08
            # gitHash: 10810480518d43dd720690e729d2f3b9a0f8eba7
            # SEED: 7977475956795433203
            ##
            source('./findNSourceUtils.R')
            Log.info("======================== Begin Test ===========================")
            simpleFilterTest_test_51 <- function(conn) {
                Log.info("A munge-task R unit test on data <test> testing the functional unit <==> ")
                Log.info("Uploading test")
                hex <- h2o.uploadFile(conn, locate("../../smalldata/chess/chess_2x2x1000/weka/test.csv.arff"), "rtest.hex")
                Log.info("Filtering out rows by == from dataset test and column \"0\" using value 0.814474112825")
                     filterHex <- hex[hex[,c(1)] == 0.814474112825,]
                    Log.info("Perform filtering with the '$' sign also")
                    filterHex <- hex[hex$"0" == 0.814474112825,]
                Log.info("Filtering out rows by == from dataset test and column \"0\" using value 0.397380498669")
                     filterHex <- hex[hex[,c(1)] == 0.397380498669,]
                    Log.info("Perform filtering with the '$' sign also")
                    filterHex <- hex[hex$"0" == 0.397380498669,]
                    Log.info("Filtering out rows by == from dataset test and column \"0\" using value 1.36338292205, and also subsetting columns.")
                     filterHex <- hex[hex[,c(1)] == 1.36338292205, c(1)]
                    Log.info("Now do the same filter & subset, but select complement of columns.")
                     filterHex <- hex[hex[,c(1)] == 1.36338292205, c(1,2)]
                    Log.info("Filtering out rows by == from dataset test and column \"0\" using value 0.759408725031, and also subsetting columns.")
                     filterHex <- hex[hex[,c(1)] == 0.759408725031, c(1)]
                    Log.info("Now do the same filter & subset, but select complement of columns.")
                     filterHex <- hex[hex[,c(1)] == 0.759408725031, c(1,2)]
            Log.info("End of test")
            PASSS <<- TRUE
            }
            PASSS <- FALSE
            conn = new("H2OClient", ip=myIP, port=myPort)
            tryCatch(test_that("simpleFilterTest_ on data test", simpleFilterTest_test_51(conn)), warning = function(w) WARN(w), error = function(e) FAIL(e))
            if (!PASSS) FAIL("Did not reach the end of test. Check Rsandbox/errors.log for warnings and errors.")
            PASS()

            ##
            # Author: Autogenerated on 2013-11-27 17:26:17
            # gitHash: a4fe16d99eb5f003dd7c7d0da17f29e1712eda0f
            # SEED: 8586424275721420395
            ##
            source('./findNSourceUtils.R')
            Log.info("======================== Begin Test ===========================")
            complexFilterTest_meexp_138 <- function(conn) {
                Log.info("A munge-task R unit test on data <meexp> testing the functional unit <['!', '!=']> ")
                Log.info("Uploading meexp")
                hex <- h2o.uploadFile(conn, locate("../../smalldata/logreg/umass_statdata/meexp.dat"), "rmeexp.hex")
            Log.info("Performing compound task !( ( ( hex[,c(1)] != 65.4272221135 )) ) on dataset <meexp>")
                     filterHex <- hex[!( ( ( hex[,c(1)] != 65.4272221135 )) ),]
            Log.info("Performing compound task !( ( ( hex[,c(3)] != 12.1504696587 )) ) on dataset meexp, and also subsetting columns.")
                     filterHex <- hex[!( ( ( hex[,c(3)] != 12.1504696587 )) ), c(1,3,2,5,4,6)]
                Log.info("Now do the same filter & subset, but select complement of columns.")
                     filterHex <- hex[!( ( ( hex[,c(3)] != 12.1504696587 )) ), c(1)]
            }
            conn = new("H2OClient", ip=myIP, port=myPort)
            tryCatch(test_that("compoundFilterTest_ on data meexp", complexFilterTest_meexp_138(conn)), warning = function(w) WARN(w), error = function(e) FAIL(e))
            PASS()

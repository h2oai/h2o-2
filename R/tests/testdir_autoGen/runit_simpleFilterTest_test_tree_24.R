            ##
            # Author: Autogenerated on 2013-11-27 18:13:58
            # gitHash: c4ad841105ba82f4a3979e4cf1ae7e20a5905e59
            # SEED: 4663640625336856642
            ##
            source('./findNSourceUtils.R')
            Log.info("======================== Begin Test ===========================")
            simpleFilterTest_test_tree_24 <- function(conn) {
                Log.info("A munge-task R unit test on data <test_tree> testing the functional unit <<> ")
                Log.info("Uploading test_tree")
                hex <- h2o.uploadFile(conn, locate("../../smalldata/test/test_tree.csv"), "rtest_tree.hex")
                Log.info("Filtering out rows by < from dataset test_tree and column \"X\" using value 1.96151645317")
                     filterHex <- hex[hex[,c("X")] < 1.96151645317,]
                    Log.info("Perform filtering with the '$' sign also")
                    filterHex <- hex[hex$"X" < 1.96151645317,]
                Log.info("Filtering out rows by < from dataset test_tree and column \"Y\" using value 0.698564082534")
                     filterHex <- hex[hex[,c("Y")] < 0.698564082534,]
                    Log.info("Perform filtering with the '$' sign also")
                    filterHex <- hex[hex$"Y" < 0.698564082534,]
                    Log.info("Filtering out rows by < from dataset test_tree and column \"X\" using value 1.20328223485, and also subsetting columns.")
                     filterHex <- hex[hex[,c("X")] < 1.20328223485, c("X")]
                    Log.info("Now do the same filter & subset, but select complement of columns.")
                     filterHex <- hex[hex[,c("X")] < 1.20328223485, c("X","Y")]
                    Log.info("Filtering out rows by < from dataset test_tree and column \"X\" using value 0.580674061076, and also subsetting columns.")
                     filterHex <- hex[hex[,c("X")] < 0.580674061076, c("X")]
                    Log.info("Now do the same filter & subset, but select complement of columns.")
                     filterHex <- hex[hex[,c("X")] < 0.580674061076, c("X","Y")]
            }
            conn = new("H2OClient", ip=myIP, port=myPort)
            tryCatch(test_that("simpleFilterTest_ on data test_tree", simpleFilterTest_test_tree_24(conn)), warning = function(w) WARN(w), error = function(e) FAIL(e))
            PASS()

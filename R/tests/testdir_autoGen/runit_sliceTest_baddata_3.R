            ##
            # Author: Autogenerated on 2013-11-29 01:20:07
            # gitHash: 10810480518d43dd720690e729d2f3b9a0f8eba7
            # SEED: 7977475956795433203
            ##
            source('./findNSourceUtils.R')
            Log.info("======================== Begin Test ===========================")
            sliceTest_baddata_3 <- function(conn) {
                Log.info("A munge-task R unit test on data <baddata> testing the functional unit <[> ")
                Log.info("Uploading baddata")
                hex <- h2o.uploadFile(conn, locate("../../smalldata/baddata.data"), "rbaddata.hex")
                    Log.info("Performing a row & column slice of baddata using these rows & columns: c(1) & c(1)")
                slicedHex <- hex[c(1),c(1)]
                    Log.info("Performing a 1-by-1 column slice of baddata using these columns: ")
                    Log.info("Performing a 1-by-1 row slice of baddata using these rows: ")
            Log.inf("End of test")
            PASSS <<- TRUE
            }
            PASSS <- FALSE
            conn = new("H2OClient", ip=myIP, port=myPort)
            tryCatch(test_that("sliceTest_ on data baddata", sliceTest_baddata_3(conn)), warning = function(w) WARN(w), error = function(e) FAIL(e))
            if (!PASSS) FAIL("Did not reach the end of test. Check Rsandbox/errors.log for warnings and errors.")
            PASS()

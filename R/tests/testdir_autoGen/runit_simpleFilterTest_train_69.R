            ##
            # Author: Autogenerated on 2013-11-27 18:13:58
            # gitHash: c4ad841105ba82f4a3979e4cf1ae7e20a5905e59
            # SEED: 4663640625336856642
            ##
            source('./findNSourceUtils.R')
            Log.info("======================== Begin Test ===========================")
            simpleFilterTest_train_69 <- function(conn) {
                Log.info("A munge-task R unit test on data <train> testing the functional unit <>> ")
                Log.info("Uploading train")
                hex <- h2o.uploadFile(conn, locate("../../smalldata/chess/chess_2x2x200/h2o/train.csv"), "rtrain.hex")
                Log.info("Filtering out rows by > from dataset train and column \"x\" using value 0.811340282349")
                     filterHex <- hex[hex[,c("x")] > 0.811340282349,]
                    Log.info("Perform filtering with the '$' sign also")
                    filterHex <- hex[hex$"x" > 0.811340282349,]
                Log.info("Filtering out rows by > from dataset train and column \"x\" using value 1.59628025199")
                     filterHex <- hex[hex[,c("x")] > 1.59628025199,]
                    Log.info("Perform filtering with the '$' sign also")
                    filterHex <- hex[hex$"x" > 1.59628025199,]
                Log.info("Filtering out rows by > from dataset train and column \"y\" using value 0.0945180229407")
                     filterHex <- hex[hex[,c("y")] > 0.0945180229407,]
                    Log.info("Perform filtering with the '$' sign also")
                    filterHex <- hex[hex$"y" > 0.0945180229407,]
                Log.info("Filtering out rows by > from dataset train and column \"x\" using value 0.754288658729")
                     filterHex <- hex[hex[,c("x")] > 0.754288658729,]
                    Log.info("Perform filtering with the '$' sign also")
                    filterHex <- hex[hex$"x" > 0.754288658729,]
                    Log.info("Filtering out rows by > from dataset train and column \"y\" using value 1.681117693, and also subsetting columns.")
                     filterHex <- hex[hex[,c("y")] > 1.681117693, c("y")]
                    Log.info("Now do the same filter & subset, but select complement of columns.")
                     filterHex <- hex[hex[,c("y")] > 1.681117693, c("color","x","y")]
                    Log.info("Filtering out rows by > from dataset train and column \"y\" using value 1.80228880498, and also subsetting columns.")
                     filterHex <- hex[hex[,c("y")] > 1.80228880498, c("y")]
                    Log.info("Now do the same filter & subset, but select complement of columns.")
                     filterHex <- hex[hex[,c("y")] > 1.80228880498, c("color","x","y")]
                    Log.info("Filtering out rows by > from dataset train and column \"x\" using value 1.54644600998, and also subsetting columns.")
                     filterHex <- hex[hex[,c("x")] > 1.54644600998, c("x")]
                    Log.info("Now do the same filter & subset, but select complement of columns.")
                     filterHex <- hex[hex[,c("x")] > 1.54644600998, c("color","x","y")]
                    Log.info("Filtering out rows by > from dataset train and column \"y\" using value 0.345419085897, and also subsetting columns.")
                     filterHex <- hex[hex[,c("y")] > 0.345419085897, c("y")]
                    Log.info("Now do the same filter & subset, but select complement of columns.")
                     filterHex <- hex[hex[,c("y")] > 0.345419085897, c("color","x","y")]
            }
            conn = new("H2OClient", ip=myIP, port=myPort)
            tryCatch(test_that("simpleFilterTest_ on data train", simpleFilterTest_train_69(conn)), warning = function(w) WARN(w), error = function(e) FAIL(e))
            PASS()

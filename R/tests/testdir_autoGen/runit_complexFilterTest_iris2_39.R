            ##
            # Author: Autogenerated on 2013-12-18 17:01:19
            # gitHash: 2581a0dfa12a51892283830529a5126ea49f0cb9
            # SEED: 2481425483200553751
            ##
            setwd(normalizePath(dirname(R.utils::commandArgs(asValues=TRUE)$"-f")))
            source('../findNSourceUtils.R')
            complexFilterTest_iris2_39 <- function(conn) {
                Log.info("A munge-task R unit test on data <iris2> testing the functional unit <['!', '!=', '&', '>=', '|', '>=']> ")
                Log.info("Uploading iris2")
                hex <- h2o.uploadFile(conn, locate("../../smalldata/iris/iris2.csv"), "riris2.hex")
            Log.info("Performing compound task !( ( hex[,c(\"Petal.Length\")] != 1.1816270194 ) & ( hex[,c(\"Sepal.Width\")] >= 2.20235240354 )) on dataset <iris2>")
                     filterHex <- hex[!( ( hex[,c("Petal.Length")] != 1.1816270194 ) & ( hex[,c("Sepal.Width")] >= 2.20235240354 )),]
            Log.info("Performing compound task !( ( hex[,c(\"Petal.Width\")] != 2.22909252156 ) & ( hex[,c(\"Petal.Width\")] >= 1.55252573886 )) on dataset iris2, and also subsetting columns.")
                     filterHex <- hex[!( ( hex[,c("Petal.Width")] != 2.22909252156 ) & ( hex[,c("Petal.Width")] >= 1.55252573886 )), c("Petal.Width")]
                Log.info("Now do the same filter & subset, but select complement of columns.")
                     filterHex <- hex[!( ( hex[,c("Petal.Width")] != 2.22909252156 ) & ( hex[,c("Petal.Width")] >= 1.55252573886 )), c("Petal.Length","response","Sepal.Width","Sepal.Length")]
            testEnd()
            }
            doTest("compoundFilterTest_ on data iris2 unit= ['!', '!=', '&', '>=', '|', '>=']", complexFilterTest_iris2_39)

            ##
            # Author: Autogenerated on 2013-11-29 01:20:08
            # gitHash: 10810480518d43dd720690e729d2f3b9a0f8eba7
            # SEED: 7977475956795433203
            ##
            source('./findNSourceUtils.R')
            Log.info("======================== Begin Test ===========================")
            complexFilterTest_iris_train_79 <- function(conn) {
                Log.info("A munge-task R unit test on data <iris_train> testing the functional unit <['!', '<', '&', '>=', '&', '<=']> ")
                Log.info("Uploading iris_train")
                hex <- h2o.uploadFile(conn, locate("../../smalldata/iris/iris_train.csv"), "riris_train.hex")
            Log.info("Performing compound task !( ( hex[,c(\"sepal_len\")] < 5.82497557487 ) & ( hex[,c(\"sepal_wid\")] >= 3.16558402527 ) & ( ( hex[,c(\"sepal_wid\")] <= 2.29497322802 )) ) on dataset <iris_train>")
                     filterHex <- hex[!( ( hex[,c("sepal_len")] < 5.82497557487 ) & ( hex[,c("sepal_wid")] >= 3.16558402527 ) & ( ( hex[,c("sepal_wid")] <= 2.29497322802 )) ),]
            Log.info("Performing compound task !( ( hex[,c(\"petal_len\")] < 3.94248911815 ) & ( hex[,c(\"sepal_len\")] >= 6.90554999413 ) & ( ( hex[,c(\"sepal_len\")] <= 4.65023289007 )) ) on dataset iris_train, and also subsetting columns.")
                     filterHex <- hex[!( ( hex[,c("petal_len")] < 3.94248911815 ) & ( hex[,c("sepal_len")] >= 6.90554999413 ) & ( ( hex[,c("sepal_len")] <= 4.65023289007 )) ), c("sepal_wid","petal_len","sepal_len")]
                Log.info("Now do the same filter & subset, but select complement of columns.")
                     filterHex <- hex[!( ( hex[,c("petal_len")] < 3.94248911815 ) & ( hex[,c("sepal_len")] >= 6.90554999413 ) & ( ( hex[,c("sepal_len")] <= 4.65023289007 )) ), c("petal_wid","species")]
            Log.info("End of test")
            PASSS <<- TRUE
            }
            PASSS <- FALSE
            conn = new("H2OClient", ip=myIP, port=myPort)
            tryCatch(test_that("compoundFilterTest_ on data iris_train", complexFilterTest_iris_train_79(conn)), warning = function(w) WARN(w), error = function(e) FAIL(e))
            if (!PASSS) FAIL("Did not reach the end of test. Check Rsandbox/errors.log for warnings and errors.")
            PASS()

            ##
            # Author: Autogenerated on 2013-11-29 01:20:08
            # gitHash: 10810480518d43dd720690e729d2f3b9a0f8eba7
            # SEED: 7977475956795433203
            ##
            source('./findNSourceUtils.R')
            Log.info("======================== Begin Test ===========================")
            simpleFilterTest_train_28 <- function(conn) {
                Log.info("A munge-task R unit test on data <train> testing the functional unit <<=> ")
                Log.info("Uploading train")
                hex <- h2o.uploadFile(conn, locate("../../smalldata/chess/chess_2x2x500/h2o/train.csv"), "rtrain.hex")
                Log.info("Filtering out rows by <= from dataset train and column \"y\" using value 1.76172017359")
                     filterHex <- hex[hex[,c("y")] <= 1.76172017359,]
                    Log.info("Perform filtering with the '$' sign also")
                    filterHex <- hex[hex$"y" <= 1.76172017359,]
                Log.info("Filtering out rows by <= from dataset train and column \"y\" using value 0.886795335778")
                     filterHex <- hex[hex[,c("y")] <= 0.886795335778,]
                    Log.info("Perform filtering with the '$' sign also")
                    filterHex <- hex[hex$"y" <= 0.886795335778,]
                Log.info("Filtering out rows by <= from dataset train and column \"y\" using value 0.784957353553")
                     filterHex <- hex[hex[,c("y")] <= 0.784957353553,]
                    Log.info("Perform filtering with the '$' sign also")
                    filterHex <- hex[hex$"y" <= 0.784957353553,]
                    Log.info("Filtering out rows by <= from dataset train and column \"x\" using value 0.735227417637, and also subsetting columns.")
                     filterHex <- hex[hex[,c("x")] <= 0.735227417637, c("x")]
                    Log.info("Now do the same filter & subset, but select complement of columns.")
                     filterHex <- hex[hex[,c("x")] <= 0.735227417637, c("color","x","y")]
                    Log.info("Filtering out rows by <= from dataset train and column \"x\" using value 0.431719000254, and also subsetting columns.")
                     filterHex <- hex[hex[,c("x")] <= 0.431719000254, c("x")]
                    Log.info("Now do the same filter & subset, but select complement of columns.")
                     filterHex <- hex[hex[,c("x")] <= 0.431719000254, c("color","x","y")]
                    Log.info("Filtering out rows by <= from dataset train and column \"y\" using value 1.99388508053, and also subsetting columns.")
                     filterHex <- hex[hex[,c("y")] <= 1.99388508053, c("y")]
                    Log.info("Now do the same filter & subset, but select complement of columns.")
                     filterHex <- hex[hex[,c("y")] <= 1.99388508053, c("color","x","y")]
            Log.info("End of test")
            PASSS <<- TRUE
            }
            PASSS <- FALSE
            conn = new("H2OClient", ip=myIP, port=myPort)
            tryCatch(test_that("simpleFilterTest_ on data train", simpleFilterTest_train_28(conn)), warning = function(w) WARN(w), error = function(e) FAIL(e))
            if (!PASSS) FAIL("Did not reach the end of test. Check Rsandbox/errors.log for warnings and errors.")
            PASS()
